---
title: "Lookup Dataset"
format: 
  html:
    theme: lux
    self-contained: true
    code-fold: true
    toc: true 
    toc-depth: 3
    toc-location: right
    number-sections: false
---

```{r setup, message = F, warning = F, fig.width = 10, fig.height = 10, echo = F}
options(scipen = 999)

library(PristineSeasR)
library(bigrquery)
library(gt)
library(gtExtras)
library(tibble)
library(tidyverse)
library(dplyr)
library(ggplot2)
library(plotly)
library(lubridate)

knitr::opts_chunk$set(eval = F, warning = F, message = F, include = F, echo = F)

ps_paths <- PristineSeasR::get_sci_drive_paths()

prj_path <- file.path(ps_paths$projects, "legacy-db")
ps_data_path <- ps_paths$datasets

bigrquery::bq_auth(email = "marine.data.science@ngs.org")

project_id <- "pristine-seas"

bq_connection <- DBI::dbConnect(
  bigrquery::bigquery(), 
  project = project_id
)
```

## Overview

The `lookup/` dataset contains reference tables used to support and standardize data entry, cleaning, integration, and analysis across the Pristine Seas Science Database. These tables function as controlled vocabularies or translation layers, enabling consistency across methods, surveys, and datasets.

Each table serves a specific harmonization role — for example, linking diver shorthand codes to taxonomic records, or defining standardized habitat categories. While these tables do not contain observational data themselves, they are essential for ensuring comparability, integrity, and interoperability across data sources.

---

## Tables

### Fish codes (`lookup.uvs_fish_codes`)

This table maps shorthand taxon_code entries recorded during underwater visual surveys (UVS) to canonical species names and taxonomic metadata. These codes are used in the field for rapid annotation and often include abbreviated or non-standard identifiers.

Each row links a field code to a taxon (species, genus, or family level), allowing traceable alignment with the authoritative taxonomy defined in `taxonomy.fish`. This mapping is essential for data integration, trait-based analyses, and reporting

### Researchers (`lookup.ps_researchers`)

This table provides a mapping between abbreviated researcher identifiers (e.g., initials used in field logs or metadata) and full researcher names and IDs. It ensures consistency in authorship, data attribution, and team tracking across datasets and expedition records.




