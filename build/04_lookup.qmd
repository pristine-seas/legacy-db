---
title: "Lookup Dataset"
format: 
  html:
    theme: lux
    self-contained: true
    code-fold: true
    toc: true 
    toc-depth: 3
    toc-location: right
    number-sections: false
---

```{r setup, message = F, warning = F, fig.width = 10, fig.height = 10, echo = F}
options(scipen = 999)

library(PristineSeasR)
library(bigrquery)
library(gt)
library(gtExtras)
library(tibble)
library(tidyverse)
library(dplyr)
library(ggplot2)
library(plotly)
library(lubridate)

knitr::opts_chunk$set(eval = F, warning = F, message = F, include = F, echo = F)

ps_paths <- PristineSeasR::get_sci_drive_paths()

prj_path <- file.path(ps_paths$projects, "legacy-db")
ps_data_path <- ps_paths$datasets

bigrquery::bq_auth(email = "marine.data.science@ngs.org")

project_id <- "pristine-seas"

bq_connection <- DBI::dbConnect(
  bigrquery::bigquery(), 
  project = project_id
)
```

## Overview

The `lookup/` dataset contains controlled vocabularies and standardized crosswalks used across the Pristine Seas Science Database. These tables ensure consistency in traits, classification, and code mappings across survey methods and spatial units.

Each table is documented in the section where it is used. This page provides a centralized index of all lookup tables.

---

## Tables

### Habitats (`lookup.habitat_types`)

This vocabulary defines the set of allowed habitat classifications for UVS (underwater visual survey) data in the Pristine Seas Science Database. These terms standardize field entries, ensure consistency in analyses, and support integration across reef and temperate systems.

The allowed terms are:

- **`fore_reef`** — Seaward-facing outer slope of coral reef  
- **`back_reef`** — Lagoon-facing side behind the reef crest  
- **`reef_flat`** — Horizontal shallow zone near the crest  
- **`patch_reef`** — Isolated reef outcrop within lagoon or sand plain  
- **`pinnacle_reef`** — Steep-sided, often isolated coral structures  
- **`rocky_reef`** — Non-coral reef formed from rock, with reef biota  
- **`reef_pavement`** — Flat, low-relief hard-bottom reef surface  
- **`channel_pass`** — Channel through reef system with strong flow  
- **`wall`** — Vertical or steep reef drop-off  
- **`kelp_forest`** — Dense canopy-forming macroalgae habitat  
- **`seagrass`** — Dominant seagrass bed habitat  


```{r}
# Define habitat vocabulary
habitat_vocab <- tribble(
  ~term,            ~description,
  "fore_reef",      "Seaward-facing outer slope of coral reef",
  "back_reef",      "Lagoon-facing side behind the reef crest",
  "reef_flat",      "Horizontal shallow zone near the crest",
  "patch_reef",     "Isolated reef outcrop within lagoon or sand plain",
  "pinnacle_reef",  "Steep-sided, often isolated coral structures",
  "rocky_reef",     "Non-coral reef formed from rock, with reef biota",
  "reef_pavement",  "Flat, low-relief hard-bottom reef surface",
  "channel_pass",   "Channel through reef system with strong flow",
  "wall",           "Vertical or steep reef drop-off",
  "kelp_forest",    "Dense canopy-forming macroalgae habitat",
  "seagrass",       "Dominant seagrass bed habitat"
)

# Define schema (only two fields, both REQUIRED)
habitat_schema <- list(
  list(name = "term", type = "STRING", mode = "REQUIRED"),
  list(name = "description", type = "STRING", mode = "REQUIRED")
)

# Create table in BigQuery
bq_table_create(
  bq_table(project_id, "lookup", "habitat_types"),
  fields = habitat_schema,
  description = "Allowed habitat classifications for UVS stations. Each row defines a unique term and its definition."
)

# Upload data to BigQuery
bq_table_upload(
  bq_table(project_id, "lookup", "habitat_types"),
  values = habitat_vocab,
  create_disposition = "CREATE_NEVER",
  write_disposition = "WRITE_TRUNCATE"
)
```

---

### Exposure (`lookup.exposure_types`)

This vocabulary defines the environmental or geomorphic exposure context of UVS stations. These terms help differentiate energy regimes and spatial positions relevant to reef structure and biological communities.

The allowed values are:

- **`windward`** — Exposed to prevailing swell or wind; typically high wave energy and surge  
- **`leeward`** — Sheltered side of reef or island protected from prevailing swell  
- **`lagoon`** — Located inside a reef or atoll, generally protected from direct wave action  
- **`channel`** — Within a reef pass or surge channel; often features strong tidal currents  
- **`sheltered`** — Protected from wave energy by a bay, spit, islet, or geomorphic feature  
- **`exposed`** — Generally high energy due to wave or swell exposure; used where windward/leeward doesn’t apply  
- **`unknown`** — Exposure not determined or insufficient data to classify 


```{r}
# Define exposure vocabulary
exposure_vocab <- tribble(
  ~term,         ~description,
  "windward",    "Exposed to prevailing swell or wind; typically high wave energy and surge.",
  "leeward",     "Sheltered side of reef or island protected from prevailing swell.",
  "lagoon",      "Located inside a reef or atoll, generally protected from direct wave action.",
  "channel",     "Within a reef pass or surge channel; often features strong tidal currents.",
  "sheltered",   "Protected from wave energy by a bay, spit, islet, or geomorphic feature.",
  "exposed",     "Generally high energy due to wave or swell exposure; used where windward/leeward doesn’t apply.",
  "unknown",     "Exposure not determined or insufficient data to classify."
)

# Define schema (two columns only)
exposure_schema <- list(
  list(name = "term", type = "STRING", mode = "REQUIRED"),
  list(name = "description", type = "STRING", mode = "REQUIRED")
)

# Create table in BigQuery
bq_table_create(
  bq_table(project_id, "lookup", "exposure_types"),
  fields = exposure_schema,
  description = "Allowed exposure classifications for UVS stations. Defines site orientation or energy context."
)

# Upload data to BigQuery
bq_table_upload(
  bq_table(project_id, "lookup", "exposure_types"),
  values = exposure_vocab,
  create_disposition = "CREATE_NEVER",
  write_disposition = "WRITE_TRUNCATE"
)
```

